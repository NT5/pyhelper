#!/usr/bin/env python
# -⁻- coding: UTF-8 -*-

import wx, sys, json
import pyhelper as py_program

# class pyhelper(wx.Frame):

	# def __init__(self, parent, title):
		# super(pyhelper, self).__init__(parent, title=title, size=(250, 200))
		# self.InitUI()
		# self.Centre()
		# self.Show()
		
	# def InitUI(self):
		# panel = wx.Panel(self, -1)
		# self.out_text_area = wx.TextCtrl(panel, pos=(5,5), size=(222, 50))

# if __name__ == '__main__':
	# try:
		# _config = json.loads( open("config.json").read() )
	# except Exception, e:
		# print "Config File error: " + str( e )
		# sys.exit()
	
	# app = wx.App(False)
	
	# py = py_program.pyhelper(_config, pyhelper(None, title='pyhelper'))

	
	# app.MainLoop()
	
	